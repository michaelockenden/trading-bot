[tool.poetry]
name = "trading-bot"
version = "0.1.0"
description = ""
authors = ["michaelockenden <michael.ockenden@hotmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"

[tool.poetry.group.dev.dependencies]
black = "^24.2.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
